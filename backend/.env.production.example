# Django Core Settings
# --------------------------
# IMPORTANT: YOU MUST REPLACE THIS VALUE with a new, long, and random secret key.
# Failure to do so is a major security risk.
DJANGO_SECRET_KEY=get_random_secret_key()
# Django Core Settings
# --------------------------
DJANGO_DEBUG=False
DJANGO_LOG_LEVEL=INFO

# Networking & Security
# --------------------------
# Comma-separated list of your production domains.
# The primary Railway domain (e.g., my-app.up.railway.app) is added automatically by the app.
# Add your custom domains here. e.g., "www.example.com,example.com"
DJANGO_ALLOWED_HOSTS=*.railway.app, localhost, 127.0.0.1



# A private, unique URL for the Django admin panel. It is recommended to change this from the default "admin/".
DJANGO_ADMIN_URL=secret-admin/

# CORS & CSRF Settings
# --------------------------
# Comma-separated list of origins allowed to make cross-site requests.
# Add your production frontend URL here. e.g., "https://www.example.com"
DJANGO_CORS_ALLOWED_ORIGINS=*.railway.app, localhost, 127.0.0.1
# Comma-separated list of trusted origins for CSRF.
DJANGO_CSRF_TRUSTED_ORIGINS=*.railway.app, localhost, 127.0.0.1

# Production Security Settings
# --------------------------
# Enforces HTTPS redirection. Handled automatically by Railway, but good to have.
DJANGO_SECURE_SSL_REDIRECT=True


# Service Connection URLs
# --------------------------
# These variables are injected automatically by Railway into the production environment.
# You do not need to set them in the Railway service variables UI.
# DATABASE_URL is set by your Postgres service.
# REDIS_URL is set by your Redis service.