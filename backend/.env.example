# Django Core Settings
# --------------------------
# IMPORTANT: Generate a new secret key for production.
# You can use an online generator or Django's `get_random_secret_key()` function.
DJANGO_SECRET_KEY= get_random_secret_key()
DJANGO_DEBUG=True
DJANGO_LOG_LEVEL=INFO

# Networking & Security
# --------------------------
# Comma-separated list of allowed hosts.
# The primary domain (e.g., my-app.up.railway.app) is added automatically by the app.
# Add any custom domains here. e.g., "www.example.com,example.com"
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1, *.railway.app

# The URL for the Django admin panel. e.g., "secret-admin/"
DJANGO_ADMIN_URL=secret-admin/


# CORS & CSRF Settings
# --------------------------
# Comma-separated list of origins allowed to make cross-site requests.
# Add your frontend URL here for development. e.g., "http://localhost:5173"
DJANGO_CORS_ALLOWED_ORIGINS=localhost,127.0.0.1, *.railway.app
# Comma-separated list of trusted origins for CSRF.
DJANGO_CSRF_TRUSTED_ORIGINS=localhost,127.0.0.1, *.railway.app

# Production Security Settings (primarily for when DEBUG=False)
# --------------------------
# Set to True to enforce HTTPS redirection. Recommended for production.
DJANGO_SECURE_SSL_REDIRECT=True


# Service Connection URLs (for local development)
# --------------------------
# These are typically provided automatically by Railway in production.
# For local development against Railway services, you can get these values
# from the "Connect" tab of your database/redis service in Railway.
DATABASE_URL=postgresql://postgres:OAMPWAJAXdGevFBSCUDhMxxrBcUxoIDs@postgres.railway.internal:5432/railway
